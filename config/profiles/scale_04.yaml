# Profile: scale_04
# Trackbar positions that produced this config:
#   num_disp(x16)=13  min_disparity=3  block_size=5  P1=3  P2=10
#   speckle_window=40  speckle_range=2  proc_scale=4(out of 9)
#
# Actual parameter values (after to_param conversion):
#   num_disparities=224  min_disparity=3  block_size=6 (SGBM forces→7)
#   p1_factor=4  p2_factor=11  proc_scale=0.5
#
# Depth coverage at proc_scale=0.5, fx≈2530:
#   fx_eff = 1265 px,  B = 0.30 m
#   closest: fx_eff*B/(min+num) = 1265*0.3/227 ≈ 1.7 m
#   farthest: fx_eff*B/min_disp = 1265*0.3/3   ≈ 127 m
#
# Usage:
#   python3 examples/stereo_disparity_node.py \
#     --ros-args --params-file config/profiles/scale_04.yaml \
#     -p show_preview:=true

/stereo_disparity_node:
  ros__parameters:
    profile: "scale_04"
    num_disparities: 224
    min_disparity: 25
    block_size: 6
    p1_factor: 4
    p2_factor: 16
    disp12_max_diff: 2
    uniqueness_ratio: 10
    speckle_window_size: 40
    speckle_range: 2
    pre_filter_cap: 63
    mode: 2
    proc_scale: 0.5
    min_depth_m: 1.7
    max_depth_m: 10.0
    show_preview: false
    preview_scale: 0.5
